@page
@model Libly.Pages.Books.IndexModel

<h1>Book List</h1>

<ul>
    @*@foreach (var name in Model.bookNames)
    {
        <li>@name</li>
    }*@

    @for(int i=0; i < Model.bookNames.Length; i++)
    {
        <p>@(i+1) : @Model.bookNames[i] </p>
    }
</ul>